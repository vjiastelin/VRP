[project]
name = "vrp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ortools",
    "requests",
    "fastapi",
    "uvicorn"
]
dev-dependencies = [
    "httpx",
    "pytest"
]
